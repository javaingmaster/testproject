package special;

import java.awt.Graphics;
import java.awt.Image;

import toolbag.Gameimage;

public class Launchpoint 
{
	double x,y;
	//Image point;
	
	public Launchpoint(double x,double y,String path)
	{
		//this.point=Gameimage.getimage(path);
		this.x=x;//-point.getWidth(null)/2;
		this.y=y;//-point.getHeight(null)/2;	
	}
	/*public void drawmyself(Graphics g)
	{
		g.drawImage(point,(int)x, (int)y, null);
	}*/
	
}
